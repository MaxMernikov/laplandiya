.index-banner
  %i.is.banner-bell
  .visible-block
    %i.is.banner-fir-tree
    .is.banner-candies= custom('index_image')
    - if custom?('banner_santa')
      %i.is.banner-bals.visible-md.visible-lg
      %i.is.banner-santa.visible-md.visible-lg
        .action
          %span= custom('banner_santa')

    .banner-action
      = custom('index_banner')
