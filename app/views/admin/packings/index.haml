.page-header
  %h1
    = I18n.t('activerecord.models.packing')
    = link_to 'new', new_resource_url, class: 'btn btn-default pull-right'

- if collection.present?
  %table.table
    %thead
      %tr
        %th= I18n.t('activerecord.attributes.packing.code')
        %th= I18n.t('activerecord.attributes.packing.show')
        %th= I18n.t('activerecord.attributes.packing.image')
        %th= I18n.t('activerecord.attributes.packing.material')
        %th
    %tbody
      - collection.each do |col|
        %tr
          %td= col.code
          %td= boolean_tag col.show
          %td= image_tag(col.image, style: 'max-height: 200px')
          %td= col.material
          %td
            .pull-right.controls
              = link_to 'edit', edit_resource_url(col), class: 'btn btn-default'
              = link_to 'delete', resource_url(col), method: :delete, class: 'btn btn-default'